import Root from "./button.svelte";
import { type ButtonProps, type ButtonSize, type ButtonVariant, buttonVariants } from "./helper";

export {
	type ButtonProps as Props,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
	Root,
	//
	Root as Button,
};
