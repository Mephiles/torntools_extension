{
  "scripts": {
	  "build": "esbuild extension/**/*.* --outdir=dist --analyze=verbose --target=chrome109,firefox128,edge109 --minify=false --loader:.html=copy --loader:.wav=copy --loader:.svg=copy --loader:.png=copy --loader:.woff2=copy --loader:.ttf=copy --loader:.json=copy --loader:.md=copy",
	  "watch": "esbuild extension/**/*.* --outdir=dist --analyze=verbose --log-limit=0 --target=chrome109,firefox128,edge109 --minify=false --watch --loader:.html=copy --loader:.wav=copy --loader:.svg=copy --loader:.png=copy --loader:.woff2=copy --loader:.ttf=copy --loader:.json=copy --loader:.md=copy"
  },
  "devDependencies": {
    "chrome-types": "^0.1.378",
    "esbuild": "^0.27.0",
    "typescript": "^5.9.3"
  }
}
