# TODO

## Release messages

Create 2 release messages: one based on a Markdown template and one based on a HTML template. Both will be in the
`/tools` folder. It should be similar to the GitHub release message. Include a value for the version and then for each
area the title and the area entries. Both messages should be added as files to the release.

Since the Markdown is meant for Discord, split the file into multiple files under the 2k character limit. Add '.... and
more (<page>/<total>)' to every message in that case, except the last where it should be 'and that's
it (<page>/<total>)'. Hold into account that those messages also increase the character limit when you split up the
files.
